{
  "name": "redux-on-state-change",
  "version": "1.0.0-beta",
  "description": "Extremely simple middleware to react to Redux state changes",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "publish": "rimraf lib && npm run build",
    "test": "mocha --recursive --compilers js:babel/register -r test/index.js"
  },
  "keywords": [
    "redux",
    "middleware",
    "redux-middleware"
  ],
  "author": "Francis John",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-eslint": "^6.0.2",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "chai": "^3.5.0",
    "eslint": "^2.6.0",
    "mocha": "^2.4.5",
    "rimraf": "^2.5.2"
  }
}
